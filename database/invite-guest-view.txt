Invite guest view (gift registry + optional who's coming)
==========================================================

1. Open Supabase → SQL Editor
2. Paste and run: database/invite-guest-view.sql
3. Hard-refresh the invite link (Ctrl+Shift+R)

This also fixes invite pages stuck on "Loading invitation..." when gift
prices / RSVP data caused the database function to error.
